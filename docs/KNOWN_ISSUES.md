# 已知问题与下一步

更新日期：2026-09-12。当前 capture-v25 在 300 秒回合取得 9.27 分，两次召集/接应确认成功，但仍 0 捕获。score-v22 保留为完整回合基线；两轮失败实验也已记录，见 [COOPERATIVE_CAPTURE.md](COOPERATIVE_CAPTURE.md)。优先级 P0 为首先解决，P1 为后续提升，P2 为工程整理。

## 算法与比赛问题

| 优先级 / 问题 | 当前证据与影响 | 建议工作与验收 |
| --- | --- | --- |
| P0：发现机保持不足、接应机未重获 | v25 两次任务均在接应机独立确认前因视觉恢复超时释放；第一名接应机初始约距目标估计位置 1174 米，理想到达 350 米范围需约 26 秒，任务约 13 秒即释放 | 优先改善持续视觉、转弯后的像素关联和接应搜索；取得真实共同帧及连续捕获，不能只延长旧坐标租期 |
| P0：双机持续捕获未实现 | 9.06 分回合 0/3 清除，三目标 `coop_ticks=0`；`OBSERVE` 只是内部意图 | 诊断发现者与接应机的共同可见窗口、到达时间、云台指向和目标关联；先让正式裁判出现有效双机跟踪，再取得连续 20 秒后的清除 |
| 环境前提：需要 Git LFS | 当前资产已上传 LFS；未安装或未完成下载时只得到指针文件 | 执行 `git lfs pull` 和 `tools/check_v22_assets.py`，再启动原版 |
| P1：单次分数缺少稳定性证据 | 目前只确认 v22 seed101 的一次非零结果 | 保留原版，记录少量不同 seed 的完整官方结果，同时比较报告误差、捕获和惩罚；不要仅筛选最高一局 |
| P1：数据少、场景相似 | 35 真车、17 诱饵、709 背景；34/34 验证来自受控相似布景 | 扩展视角、距离、遮挡和未见布景的人工审核数据；按真实场景隔离验证，分别统计误报和漏报 |
| P1：严格报告窗口容易错失目标 | 要求连续身份、运动、姿态和定位有效；慢车可能不满足 3.5–18 米/秒门限 | 从单帧拒绝原因与轨迹中断入手；避免只降低置信度。v15/v19 各一次错误报告曾产生约 1577/1372 米 RMSE |
| P1：相机时刻与定位未全面标定 | `capture_verified=False`，接收位姿未证明等于拍摄位姿；局部水平平面不适应所有地形 | 检查图像延迟、光轴姿态与局部地形，用独立公开观测验证定位；110 米是工程筛选量，非概率精度保证 |
| P1：背景视差可能被误认为运动 | 单应变换不能完全解释地面、树冠、车辆等不同高度 | 分析静态背景残差与真实车辆残差，增强运动判断并检查假阳性，不把“移动”直接等同于“真车” |
| P1：规划与真实飞行动力学有偏差 | 枚举轨迹用近似模型，通过前方航点执行；一次零惩罚不证明安全 | 检查实际航迹、最小机间距、越界和接应可达性，同时验证捕获收益与安全 |
| P2：不是完整 YOPO 端到端方法 | v22 只有外观分类学习，轨迹代价未反传到视觉；历史评分头未参与得分回合 | 如果重启该主线，另建实验，验证任务梯度、固定翼动作约束及官方成绩，保留解析基线作对照 |

## 运行和工具的具体限制

| 现象 | 原因 / 处理方法 |
| --- | --- |
| `Missing learning environment` | `vision.cmd` 只找本仓库 `.venv-learning`；按 [QUICKSTART.md](QUICKSTART.md)重建 |
| 找不到 `competition` 或官方必需文件 | 项目不在发行包内部，或只有 Git 源码；先准备完整官方环境。部分运行入口支持 `--sim-root`，但隔离测试和导出仍使用父目录，因此整体交接采用标准目录布局 |
| 找不到 `vision.pt` 或 hash mismatch | 缺少冻结资产，或混用了不同目录的 `agent.py` 与权重；按清单同步并核对，不修改哈希绕过检查 |
| 导出时找不到 `competition-guidance-v3.py` | 默认值已修正为当前冻结包；如果旧命令还显式传入历史路径，改为 `--controller artifacts/submission/score-v22/agent.py` 复用冻结包内函数 |
| 隔离检查找不到 `.image` | `check_visual_package.py` 依赖固定受控真车照片，该照片已通过 Git LFS 提供；先确认完整下载，不能因此说包推理失败 |
| 训练找不到原机 D 盘图片 | 审核 JSONL 保存了绝对路径，训练脚本直接使用 `image` 字段；按资产清单生成本机副本并核对图片哈希 |
| 同步 JSONL 后仍无法训练 | 还必须同步它引用的图片；源图和审核记录不是同一个文件。脚本不会自动下载数据 |
| v22 正常运行却完全不报告 | 先确认 `--enable-reports`：本地 runner 会覆盖包的默认值；再区分“候选不足”和“被门限拒绝” |
| 改了 `src/`，结果/行为却没改变 | `--submission` 运行的是导出副本；重新导出并选择新包，检查 `runner-call.json` 哈希 |
| 输出目录已存在 | 入口防止覆盖旧结果；换新时间戳目录，不删除已有实验来重用名字 |
| 6379 占用、UE 启动失败 | 当前视觉运行使用固定端口；核对是否已有自己/队友的回合运行，检查本次日志，不全局清理进程 |
| 裸 `run.cmd agent --agent zqhj_entry:EntryAgent` 与 v22 不同 | 这是较早解析入口，不是已冻结的完整视觉得分包；v22 用 `vision.cmd run --submission ...` |
| 旧文档写着 YOLO、0 分或其他门限 | 冻结提交包文档是当时快照，当前主文档已统一为 v22；使用 README、HANDOFF 和 FIRST_SCORE |

## 下一轮建议交付

1. **复现可交付**：新队友完成资产同步、哈希检查、依赖检查及冻结包运行；记录新机器差异。
2. **捕获可诊断**：用回合结束后的公开照片、动作及外部判定记录解释两机为什么未共同跟踪，不能仅凭 `OBSERVE` 日志判断。
3. **改进可验收**：一次只围绕一个主要假设修改，导出新包并保存正式 JSON。以至少一次真实清除为捕获里程碑，兼顾坐标精度和惩罚。
4. **高分可统计**：捕获链路打通后再扩展多 seed 和未见场景验证，报告整体表现，而不是最高单次结果。

v22 原始成绩保持不变；capture-v23 的新回合单独记录，不以状态机测试代替性能结论。
