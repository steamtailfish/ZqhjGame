# 首个分数：上报链路诊断

最新：v21完整正式回合仍0分、0报告。v22调整连续身份计数和定位预算后，正式600秒回合最终9.06分、2报告、0清除、0惩罚，定位RMSE为11.27米。分数来自坐标精度，尚未完成双机捕获。至此停止新增优化，保留冻结版本供验收。

2026-09-11。用户要求先取得正式非零分。本页记录失败原因和修复依据；只有原始官方 evaluation 是比赛成绩。

## v18：定位已经出现，但没有上报

正式 seed101、600秒，0分、0报告、0清除、0惩罚。三机选中候选帧为233/185/239，有地理估计89/64/79帧；运动、地理与身份同时通过的帧仅10/7/13，未形成导航模块要求的连续地理轨迹。

以第三架飞机的一段真实公开照片序列为例：

| 日志时刻（秒） | 像素连续帧 | 相对背景运动连续帧 | 真车置信度 | 地理工程预算（米） |
| --- | --- | --- | --- | --- |
| 231.05 | 1 | 0 | 0.9979 | 无估计 |
| 231.58 | 2 | 1 | 0.9981 | 69.56 |
| 232.10 | 3 | 2 | 0.9966 | 67.33 |
| 232.62 | 4 | 3 | 0.9992 | 73.17 |
| 233.13 | 5 | 3（旧证据） | 0.9929 | 无估计 |

云台从pan 0度跳到约46.8度，随后约51.4度，再退回搜索角度，地理估计中断。由于导航轨迹尚未收到3次有效候选，独立上报仍被local_track_id为空阻断。

回合结束后，外部诊断把232.62秒的公开坐标估计与最近时间的裁判记录作对照：时间差约0.067秒，与该真实车辆位置相差约4.54米。这个数是单点事后诊断，既不是全局精度，也不是正式分数；当时没有发送报告。裁判记录和目标位置没有进入在线代码或训练标签。

## 修复及验证范围

v19使用图像单应变换关联转动后的目标位置，云台每次pan/tilt最多改变6/3度，保留已验证目标用于云台稳定。报告仍要求新鲜运动证据。

v20准备了独立的短轨迹报告路径：4帧连续高置信真车身份（≥0.95、类别分差≥0.9），至少2个相邻、位置一致的有效地理估计，预算≤90米、源照片新鲜度≤0.6秒，并要求按同一图像地面平面推算的相对背景速度在3.5–18米/秒范围。该速度条件用于排除低幅度背景视差和跨物体跳变，是当前有限观测下的工程门限，可能漏掉慢车或急转目标。报告不再必须等待导航轨迹确认，且与普通报告共享1Hz限制。

v19完整回合已结束：0分、1报告、0清除、0惩罚，报告RMSE为1371.89米。其最长像素轨迹达到19帧，但错误报告仍经旧地理轨迹路径发出，当前框置信度约0.926。另在第二架飞机436.33秒的真车定位中，事后同时间样本对照误差约6.20米，仍未上报。

因此v21关闭旧地理轨迹报告路径，只使用上述严格像素轨迹条件；协同导航仍使用地理轨迹。v21隔离检查通过，受控真车密集照片回放12次报告、诱饵回放0次报告；正式600秒回合运行中。回放仍不构成正式得分。

证据：`artifacts/checks/score-v18-101-analysis.json`、`score-v18-101-judge-analysis.json`、`score-v18-effective.png`，原始公开照片/日志位于`artifacts/vision/runs/score600-v18-seed101/observations`。训练与推理命令见`../FIRST_SCORE.md`。
