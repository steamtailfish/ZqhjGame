# 项目规划（依据实际发行包修订）

本轮已完成（2026-09-11）：v22冻结包600秒正式回合最终9.06分、2报告、0清除、0惩罚，报告定位RMSE 11.27米。交付与本回合哈希一致的包和训练推理命令，停止追加优化。双机20秒捕获仍留作后续目标；一次非零实测不等于稳定高分。最新命令和官方证据见[FIRST_SCORE.md](FIRST_SCORE.md)。下方内容为历史规划。

最新优先级：以实际比赛得分验收。score-v12的600秒正式回合仍0分、0清除、0报告、0惩罚；外部判定补录表明未发生真实真车锁定。下一阶段应先验证一般化搜索覆盖、实际车辆可见性与识别，再验证双机同目标20秒。不能将OBSERVE状态、背景误检减少或小样本mAP当成捕获成功。当前命令与结果见SCORE_OPTIMIZATION.md。

最新视觉增量：64张受控公开RGB裁剪已审核，完成30epoch双类训练；条带搜索、因果平面估计、有条件上报、fly_to导航及完整视觉包已实现。旧版本600秒正式场景完成但0分，最新60秒产生1个地理候选但仍未完成K=2。下一验收项是不同布景的识别泛化、采集姿态/绝对定位误差、连续多机同目标观察和最新包完整成绩；不以等待用户提供路径作为前提。详见docs/VISUAL_CLOSED_LOOP.md。以下历史规划不得覆盖最新事实。

v0.3增量：已实现真实公开观测采集、按回合隔离训练/验证、51维边界与机体系合同、续训、单文件嵌入权重导出和真实引擎网络推理。详见TRAINING_AND_INFERENCE.md和STATUS.md。该控制链路通过不代表D视觉、真假识别、上报和K=2成绩验收通过。

后续增量：用户明确询问使用YOPO引导学习，已加入独立离线学习模块并完成40步合成梯度验证，见docs/GUIDANCE_LEARNING.md。先前“暂不训练”更新为不默认进行大规模/赛事训练；本次小规模机制验证已训练网络，但未接入在线Agent，也未推进视觉/真实K=2验收。

2026-09-10增量：用户明确要求直接依照Gou与YOPO两篇论文开始构建。已将A的时间/检测/广播适配、双机候选仲裁、APF引导及固定翼候选轨迹筛选实现为解析基线，入口不再是空动作。详见docs/PAPER_GUIDED_IMPLEMENTATION.md。此为开发推进，不改写下列阶段验收；D在线视觉定位、真实K=2、真假识别、训练与提交导出仍未通过/未执行。

目标赛题：红枫2026赛题二，多机协同识别。默认工程位置为官方环境/ZqhjGame。2026-09-07晚更新：用户决定D感知验证前置、A作为主路线、B保留后续局部控制改进；暂不移植完整YOPO/Tracker、不训练、不批量调参。

本轮已完成一次seed42、60秒的UE三机公开photo与控制探针。结果见docs/PERCEPTION_CONTROL_VALIDATION.md：图像JPEG 1024×768、基础控制、广播去重通过；定向投递未通过；真实图像到目标坐标尚未验证。双机共同观察本轮未启动。下表阶段5的**图像通路部分已前置执行**，识别与定位仍待完成；不能再将整项视觉工作推迟到阶段4之后。

依据：手册V2.0.2完整内容、OpenSim2.0.3 SDK/runner/当前评分实现及本轮真实试验。详见docs/ENVIRONMENT_AUDIT.md、docs/SDK_CONTRACT.md。未找到之前的ZqhjGame_Codex_Project_Plan.md，以下是经证据约束的新规划，不能声称与旧文件逐条对比。

## 必须改变的设计假设

- 从K=2同步跟踪出发，不能沿用官方基线的三机分区独立锁定、K=1清除推断。至少两机对同一候选持续观测，第三机角色可以先简化。
- 真假目标均移动，不能仅靠“在动”判真；无稳定目标ID，位置有噪声，需要短轨迹关联及不确定性管理，不能读裁判ID。
- 先做合法时间适配和通信收件去重；固定dt不是仿真经过时间，inbox不是当拍新消息，广播有自回环，统计是累计。
- 上报独立影响30%精度，且团队对同一裁判目标共享1Hz配额。应协调唯一上报者、覆盖各目标，避免盲目把疑似诱饵上报或每机重复抢配额。
- 无目标清除标志，只能从允许的运动历史、全局聚合清除数量变化等提出有置信度的退出判断，不能把总体数量变化强行绑定当前轨迹。
- 先确认云台角度、500m高度和图像归属，再做几何反算/视觉扩展；不把SDK辅助函数或基线中的200m假设原样复制。
- 不优化本地time原点和按事件扣分的缺陷。用正式手册约束设计安全间距/时间预算，保留本地评分差异报告。
- 同seed只保证部分随机过程；评估关注证据完备和统计稳健性，最终用未见路线/不同场景验证，不记忆固定种子路线。

## 阶段与验收

| 阶段 | 最小交付 | 验收标准 |
| --- | --- | --- |
| 0 环境审计（本轮） | 5份核心工程文档、完整手册提取、版本哈希、路径自定位入口、单模块加载壳、官方基线记录 | 官方文件无修改；导入/合成/dry/真实分层；有600秒原始官方评分才称完整回合；失败与未执行项明确 |
| 1 合法观测适配 | 实例内相对仿真时钟；单/多检测归一化；消息序号、TTL、去重、自消息策略和累计统计差分 | 首帧None、重复帧、间断、reset均可处理；有效业务载荷≤50 UTF-8字节；不把一条旧消息当多条信息；无Redis/文件读写进入在线模块 |
| 2 单机可控观测闭环 | 保守飞行/盘旋、合法位置局部坐标换算、云台单指令响应核验、含噪短轨迹 | 在真实引擎小回合中验证角度/转速/FOV；无越界、满足速度/高度；无固定目标ID；不以运动直接判真 |
| 3 最小双机协同 | 发现者发布带时间的候选，第二机接应；两机保持间距、指向共同候选，第三机先保守搜索 | 单次真实回合出现至少一个由官方裁判确认的K=2清除；离线解释跟踪中断原因；没有改裁判或真值输入 |
| 4 上报与任务切换 | 简单角色仲裁、唯一上报者、轨迹关联/重复目标抑制、清除后的不确定性处理 | 目标是三个目标均有有效上报；先核验主办方是否提供逐目标误差导出（本版JSON未输出），可得证据不足时不宣称已证明每目标覆盖；结合accuracy、安全和清除指标 |
| 5 视觉可达性与真假辨别 | UE三路photo归属/格式/新鲜度实验；按需要加入最小识别器 | 三台合法obs均取得正确图像才进入视觉算法；解释缺帧、延迟和定位误差；核验eval默认首机绑定限制；不读截图目录 |
| 6 小规模回归 | 少量固定版本/命令/种子的对照，未见路线验证；必要时才讨论训练 | 每次保留原始官方JSON、模式、天气、依赖、种子、耗时；不宣称同seed完全确定；未解决裁判冲突另列 |
| 7 提交导出 | 多模块开发产物导出单个Agent模块；依赖清单、≥2000字技术报告、加载说明 | 单模块在干净PYTHONPATH中可被官方加载，三实例独立reset；与开发版命令输出等价检查及真实回合回归；符合主办方最终接口 |

当前顺序：D三机图像通路（本机已验证）→D最小单机视觉候选定位核验→A时间/广播消息适配及基础控制→A最小双机观察→任务切换/上报→小规模回归。正式图像配置、权重提交和默认eval多机绑定须并行向主办方确认。双机实验分别验收“控制共同观察”和“真假识别”；官方per_target/清除输出才是裁判证据。公开send_to投递未通过，A的初期协调可使用已验证broadcast加逻辑收件人，不猜测引擎私有参数修复send_to。

## 模块边界（规划，不预建空文件）

在线入口只负责生命周期与串联。将来按实际规模再拆分：观测/时间适配、消息协议、候选轨迹、飞行/云台控制、协同状态机、上报调度。所有可变状态归属单个Agent实例；队友信息只从通信载荷来。算法仅依赖公开SDK和可提交依赖，不导入runner、world_state、裁判或工程运行工具。

离线工程层保留在tools：定位路径/解释器、管理本次子进程、记录运行证据、结果摘要、导出与验证。可以读公开场景和评分用于解释实验，但不得把动态真值、路线或固定ID生成成在线常量。现阶段只有src/zqhj_entry.py空动作入口，不创建空算法目录。

## 评价口径与当前限制

每次同时报告：完整回合是否结束、清除数/3、kill/accuracy/time各维度、总报告数与总体RMSE、per_target跟踪诊断、惩罚次数及总分、异常日志和墙钟耗时。本版JSON未导出每目标报告数与RMSE，列为后续合法离线诊断待解决项，不把总体RMSE当各目标精度。区分本地train真实引擎成绩、UE视觉eval和正式赛事结果。

官方基线仅作为运行参照，其K=1旧假设、移动诱饵不适配和云台高度假设不应变成项目设计依据。短回合0分不能说明引擎失效；完整回合的0分也不能用来降低赛题二验收要求。

需要主办方澄清的优先项：惩罚持续口径、任务耗时原点、mission_area与评分边界、云台坐标/速率、正式多机图像和eval加载/提交命令。本轮不修改官方文件去修这些问题，也不替主办方发布规则。

下一步最优先的一个功能：**本机photo到单个候选位置的最小定位探针**。先验证像素方向、云台/机头坐标关系、实际FOV和地面投影；无可靠候选时不输出坐标。它只产候选及不确定性，不判定真车、不启动搜索分配，不直接复用默认YOLO的首机图像和固定60/45度FOV假设。定位条件通过后，再用相对仿真时间与保守2Hz广播做一次双机观察。
