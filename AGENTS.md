# ZqhjGame 工作约定

- 最新决定（2026-09-13）：用户明确要求“先把最好方案的代码提交同步”。本次发布基准是冻结capture-v31；master默认src的17个导出模块须与其manifest的sources哈希相同。28.33分属于该v31原始包，不能移用于新模型或后续候选。
- v31：seed101完整600秒请求/599.9833秒记录，28.33分、4报告、RMSE6.5411588239米、0/3捕获、0惩罚、passed=false。只是最好单次记录，尚无多seed稳定性或20秒捕获；同seed诱饵每轮仍随机。
- 默认方案是轮廓候选、64×64局部CNN、图像运动几何定位、搜索/验证/广播接应状态机、解析固定翼基元规划。近圈采用四秒圆弧终点及匹配曲率候选。当前不启用YOPO学习评分头或Nano图像提议。
- v26为历史已发布18.67分基准，score-v22为冻结9.06分基线及导出依赖。保留这些包与现用appearance-v3训练依赖。不要按旧名称删除照片；accepted-local.jsonl是本机训练清单，不提交。
- 本次只发布v31及适用的运行/训练/导出、诊断和回归代码，后续v32/v33/v34及困难负样本实验仍留本机。v32为9.49分，v33/v34各0分，均无捕获；不把零分归因于未触发的新分支。
- 发布准备前的v33开发源与Nano导出器、旧包说明备份在.local-archive/release-v31-preparation/development-before-release。额外未提交实验文件仍在工作树，不能混入默认v31发布或误当删除对象。
- 用户另要求清理先给清单，尚未确认A/B/C删除范围，未执行该清理。此次提交授权不等于删除历史资料。
- 用户此前要求本地迭代验证后再上传；本次v31发布已明确授权，后续新候选继续本地评测，不逐候选推送大文件。
- 项目和Git根目录为ZqhjGame，分支master；官方SIM_ROOT默认为父目录。自研写入仅在项目内，官方SDK、exe、原场景和评分只读。保留其他未提交修改，不执行reset/clean、历史重写或全局进程清理。
- artifacts使用Git LFS并保持-text；src/zqhj_*.py保持原始字节以便源哈希和跨机器导出复核。本机虚拟环境、缓存、.local-archive不提交。不要改已评分Agent、vision.pt和原始evaluation；发布说明/元数据变更须备份并更新相应哈希。
- 在线Agent仅用obs.self、obs.comm_inbox、obs.briefing和实例状态；不得读取Redis、隐藏路线、裁判真值、其他Agent对象或磁盘截图作为在线输入。裁判记录只供赛后诊断，不自动生成身份训练标签。
- 赛题二为3机，至少2机同目标20秒，短中断容忍2秒；通信50字节/4Hz，高度500米，速度15–40米/秒，FOV5–50度。正式分数以原始evaluation为准，本地可见/joint、回放、mAP和软件测试不能替代。
- 修改src后导出新目录，不覆盖冻结包。导出器默认控制函数来自score-v22/agent.py，合作捕获分支使用解析规划，不调用其中历史学习评分头。
- 时钟微测在本机自建一机场景完成：time_scale1/2/4当前配置路径均约0.538倍实时；67Hz短测约0.743倍但改变物理dt。未在正式比赛启用步频/时钟修改；所有微测进程已结束。
- 当前命令以README、docs/QUICKSTART.md和docs/RELEASE_V31.md为准，历史研究文档必须按其时间理解。发布前验证源码哈希、独立包、重导出、测试和文档链接；原始成绩保留，不为同步代码重复运行600秒比赛。
